# Progress

- Inspected the paper and current entry scripts.
- Identified safe naming updates that do not alter checkpoint state_dict keys.
- Identified stale stage-2 profiling/debug code and README structure as the main cleanup targets.
- Updated entry scripts to use paper-aligned public names for the Stage 1 uncertainty estimator and Stage 2 UDBM bridge.
- Removed disabled profiling blocks, import-time debug prints, and commented-out debug statements from UDBM model files.
- Added public model aliases while preserving original class names for checkpoint compatibility.
- Cleaned dataset and metric helper comments/logs to remove stale debug output and non-English public messages.
- Rewrote README in a GitHub-style structure with installation, dataset layout, pretrained weights, training, evaluation, and citation sections.
- Verified Python syntax with `python -m py_compile`.
- Verified that no common absolute paths remain in Python/Markdown/TeX files.
- Verified no Chinese text remains in the cleaned public Python/Markdown files.
- Removed `__pycache__` directories produced by the verification step.
- Verified the cleaned project in conda environment `fcl` using PyTorch 1.12.1+cu116.
- Confirmed `train_s1.py --help`, `train_s2.py --help`, and `test_s2.py --help` run under `fcl`.
- Fixed `metrics/metric_util.py` so `metrics` imports without the missing root-level `matlab_functions.py`.
- Confirmed S/M/L model modules import and expose the paper-aligned aliases.
- Confirmed UDBM-S minimal Stage 1 and Stage 2 forward passes return `(1, 3, 32, 32)`.
- Confirmed all pretrained checkpoints can be read and contain `step`, `model`, `opt0`, `ema`, and `scaler` keys.
- Replaced the L Stage-2 checkpoint with the matching NAFNet-48 checkpoint from the original workspace and confirmed strict state_dict loading for all S/M/L Stage 1 and Stage 2 models.
- Added shell wrappers under `scripts/`: `train_s1.sh`, `train_s2.sh`, `train_two_stage.sh`, `test_pretrained.sh`, `test_checkpoint.sh`, and shared `common.sh`.
- Updated README with shell script usage, common environment variables, and examples for training/evaluation.
- Verified shell scripts with `bash -n` and `CONDA_ENV=fcl` `--help` smoke tests.
- Added root-level POSIX `sh` scripts for direct use: `train_s1.sh`, `train_s2.sh`, `train.sh`, `test.sh`, `test_real.sh`, `test_cd11.sh`, and `test_checkpoint.sh`.
- Updated README to prioritize direct commands such as `CUDA_VISIBLE_DEVICES=0 sh test_real.sh`.
- Verified root-level scripts with `sh -n` and `conda run -n fcl ... sh <script> --help` smoke tests.
- Removed additional commented-out code remnants from `src/` and `src_s1/` model files while keeping short comments that explain active logic.
- Renamed the Stage 2 sampling method from `ddim_sample_uncertain_diffuir` to `sample_uncertainty_aware_bridge` in all S/M/L model variants.
- Verified that the old sampler name has no remaining references and that no common absolute paths are present in Python, Markdown, TeX, or shell files.
- Re-verified the cleaned model files in conda environment `fcl` with `python -m py_compile`, import/name checks, a minimal Stage 2 forward smoke test, and strict checkpoint loading for all UDBM-S/M/L Stage 1 and Stage 2 pretrained weights.
